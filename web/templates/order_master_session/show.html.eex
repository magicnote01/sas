<h2>Order Master Session</h2>

<%= render "base_show.html", order_master_session: @order_master_session %>

<%= link "Close Session", to: order_master_session_path(@conn, :close, @order_master_session), class: "btn btn-primary" %>

<br /><br />

<%= link "Back", to: order_master_session_path(@conn, :index) %>
