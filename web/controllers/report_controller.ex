defmodule Sas.ReportController do
  use Sas.Web, :controller

  def search(conn, _opts) do
    # show search
  end
  def process_search(conn, _opts) do

  end
  def show_result(conn, _opts) do
     
  end
end
